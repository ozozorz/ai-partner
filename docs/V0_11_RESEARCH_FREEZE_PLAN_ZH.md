# AI Partner v0.11 研究冻结与投稿执行计划

> 状态：执行中  
> 起始日期：2026-07-22  
> 目标：建立与当前实现一致、可预注册、可独立复验的 KCGS 期刊证据包。  
> 约束：v0.4 只作为 pilot；任何 v0.11 确认性结果不得覆盖或反向修改预注册主假设。

## 阶段 0：基线审计与失效关闭

退出条件：工程、旧证据和官方要求有可追踪审计；已知远程配置失败不会意外启用 LLM。

- [x] 固定审计基线提交 `87819185fe7ef703b2880ab2362f7ad210b4ca36`；
- [x] 重建 v0.4 主实验、A2 和离线分析；
- [x] 核对稳定输出哈希；
- [x] 运行 97 个既有测试；
- [x] 实现配置失效关闭与远程 HTTPS 策略；
- [x] 新增 5 个端点策略测试，完整 102 个测试通过；
- [x] 更新当前 KCGS 审计。

## 阶段 1：研究边界与契约正确性

退出条件：v2 结构验证、Java 解析和执行前置条件对论文范围内任务给出一致结论。

- [x] 把 v1 明确标记为 v0.4 历史协议，不改写其冻结语义；
- [x] 为 v2 `MaidControlIntent` 固定参数为零/非零、必填字段、协议枚举和重命名文本规则；
- [x] 建立 28 组 Schema↔codec 差分测试：有效样本、无效样本和边界值；
- [x] 按任务完整剩余请求数量验证背包/容器容量；
- [x] 持久化完整契约谓词、剩余超时预算与恢复预算；
- [x] 对动态目标替换使用统一且可记录的契约级恢复上限；
- [ ] 由学生与导师确认论文任务集合；建议主实验只包含 `COLLECT_BLOCK`、`DEPOSIT_ITEM` 和 `COLLECT_AND_DEPOSIT`，产品其他功能只作系统背景。

当前验证：144 个 JUnit 测试、5 个 Python 分析测试全部通过。TODO：任务范围经导师确认后写入外部时间戳预注册。

## 阶段 2：可归因实验运行器

退出条件：一个冻结候选可以字节一致地输入所有变体，且每个消融只改变一个预注册机制。

- [ ] 定义候选语料记录格式：指令、世界摘要、原始响应、解析结果、模型元数据、SHA-256；
- [ ] 实现候选生成阶段与执行重放阶段分离；
- [ ] 禁止系统变体在配对执行时重新调用模型；
- [ ] 实现 `SCHEMA_ONLY`、`IBC_PRE`、`IBC_MONITOR`、`IBC_FULL`；
- [ ] 在批次清单中记录提交、Schema、场景、候选库、配置和分析版本哈希；
- [ ] 用单元测试证明各变体除目标机制外配置一致。

## 阶段 3：独立安全账本与自动世界测试

退出条件：故意注入的每一种越界行为都能被账本检测，关键世界状态转换可自动重复。

- [ ] 记录测试区内外所有受监控方块变化；
- [ ] 记录物品来源、目标、数量和容器身份；
- [ ] 记录实体伤害、死亡、权限/所有者检查和终态后动作；
- [ ] 将安全判定与系统自己的成功/失败码分离；
- [ ] 增加变异测试：区域外破坏、错误物品、超量、错误容器、终态后动作；
- [ ] 增加 Fabric GameTest：区块卸载、重启恢复、容器替换、多女仆竞争；
- [ ] 增加长时间运行与资源泄漏/每 tick 开销采样。

## 阶段 4：预注册与小规模 pilot

退出条件：主假设、指标、排除规则、样本量和停止规则获得导师确认并带可信外部时间戳。

- [ ] 由 v0.5 历史草案重建 v0.11 预注册；
- [ ] 明确主要终点、次要终点和安全终点；
- [ ] 以场景/指令簇为独立统计单位，不以重复 episode 伪增样本量；
- [ ] 预先指定分层/聚类置信区间、效应量和多重比较策略；
- [ ] 用不进入主分析的 pilot 验证运行器和检测能力；
- [ ] pilot 后只允许修复工具问题，任何协议变化均重新冻结和预注册；
- [ ] 在 OSF、Zenodo 或学校认可平台登记不可变时间戳。

TODO：样本量必须由最终主要终点和 pilot 方差决定，不能沿用 v0.4 的 3 次重复习惯值。

## 阶段 5：确认性实验与盲化分析

退出条件：所有计划批次完成，原始日志只追加，报告可从空构建目录一键重建。

- [ ] 在主实验开始前锁定代码和所有指纹；
- [ ] 按预注册随机化/平衡顺序运行；
- [ ] 记录失败、重试、模型成本、延迟和环境异常；
- [ ] 不删除失败 episode；排除必须匹配预注册规则；
- [ ] 由原始日志生成表格、图和统计结果；
- [ ] 独立复核一份随机抽样日志及全部安全违规；
- [ ] 发布带哈希和版本号的研究制品。

## 阶段 6：KCGS 稿件与提交

退出条件：技术内容、格式、伦理和上传条件全部通过 Go/No-Go 审查。

- [ ] 形成约 10 页韩文或英文正文；
- [ ] 韩文摘要 200–350 字符、英文摘要 300–600 字符；
- [ ] 使用 3–7 个韩英一致关键词；
- [ ] 所有图表内容与标题为英文；
- [ ] 参考文献按正文出现顺序编号并以英文书写；
- [ ] 报告负面结果、协议偏离和外部效度限制；
- [ ] 核验全体作者会员、贡献、利益冲突、资助和伦理声明；
- [ ] KCI 相似度低于风险阈值并归档报告；
- [ ] 以当期正式公告核验截止日期、费用、模板和上传入口。

## 当前下一动作

1. 实现冻结候选重放和四条件消融；
2. 建立独立安全账本与 GameTest；
3. 由导师确认论文任务集合与主要终点；
4. 上述门槛全部通过后，才允许完成并发布 v0.11 预注册。
